# 轻笺产品需求文档（PRD）

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 产品名称 | 轻笺 |
| 文档版本 | V0.4 当前实现基线 |
| 产品形态 | Windows 个人桌面工具 |
| 文档日期 | 2026-08-23 |
| 当前状态 | 本地桌面工具持续迭代，任务状态与多屏体验已进入验证阶段 |

本文档用于统一轻笺的产品定位、功能边界、交互规则和验收标准。运行方法见 `README.md`，技术实现另见后续 `TECH_DESIGN.md`。

## 2. 产品背景

用户需要一款轻量、透明、可置顶和贴边隐藏的 Windows 桌面工具，用于管理临时备忘、时间提醒和长期学习记录。现有同类软件可能存在订阅收费、功能过重或数据不够自主的问题，因此轻笺采用本地优先、离线可用的设计。

## 3. 产品定位

> 轻笺是一款面向个人用户的 Windows 桌面信息管理工具，用于短期备忘、时间提醒和长期学习积累。

核心价值：

- 快速记录，不打断当前工作。
- 在正确时间重新呈现重要内容。
- 将短期事项与长期知识分开管理。
- 数据保存在本地，由用户掌控。
- 作为桌面侧边工具长期驻留，但尽量减少遮挡。

## 4. 目标用户

### 4.1 核心用户

- 同时处理多个项目、容易遗漏临时事项的知识工作者。
- 需要持续积累 AI、建筑、工程或产品知识的学习者。
- 偏好 Windows 桌面工作流、希望数据本地保存的个人用户。

### 4.2 典型场景

1. 临时记录“下午提交项目材料”，并设置提醒。
2. 每周一、三、五提醒学习 Agent 产品设计。
3. 将学习内容写入笔记，归入“AI Agent”分类并添加标签。
4. 查看某一天的全部提醒及法定节假日信息。
5. 标记“今日已积累”，查看连续学习天数。
6. 将窗口置顶或贴边隐藏，需要时快速唤出。

## 5. 产品原则

1. **便签负责近期要做什么。**
2. **笔记负责长期积累什么。**
3. **提醒负责什么时候将内容重新带到用户面前。**
4. 本地优先，核心功能不依赖网络。
5. 快速操作优先于复杂配置。
6. 提醒数据只维护一份，通过来源关联便签或笔记。
7. 新功能不得破坏既有数据，应提供自动迁移。
8. 录音必须持续提供清晰、可见的状态提示，不提供隐蔽录音或规避会议软件检测的能力。

## 6. 产品范围

### 6.1 当前范围

- Windows 单机桌面应用。
- 提醒、便签、笔记、新闻、设置五个顶部板块。
- 本地数据存储。
- 日历、农历、周期提醒、分类标签和学习记录。
- 系统托盘、通知、开机启动及桌面窗口能力。

### 6.2 暂不纳入

- 多人协作与团队权限。
- 云端同步和跨设备同步。
- 移动端、Web 端和 macOS 客户端。
- 公开分享、评论和社交功能。
- 复杂项目管理、甘特图和审批流程。
- 通用 RAG 和知识库问答；后续单独评估。

### 6.3 面试录音与复盘（试验功能）

- 笔记工具栏保持单一“AI面试复盘”入口。
- 同时录制麦克风输入与 Windows WASAPI 耳机/扬声器回环，两路音频独立保存。
- 采用“停止录音后转写”，使用本地 SenseVoice-Small，不上传录音。
- 转写内容可追加到当前笔记，再调用已配置的 Step Plan `step-3.5-flash` 模型生成摘要、STAR案例和改进建议。
- 面试录音时可启用准实时提问检测：会议方音轨默认由本地 SenseVoice 识别，连续讲话按最长15秒的内部块处理并保留约1秒重叠；达到块上限只表示续段，不得触发候选问题。系统将连续识别文本拼接为最长约75秒的完整问题，遇到约1.3秒最终停顿或达到长度上限后才检测提问、开放发送按钮并预检索“面试知识库”。自动模式仅在本地识别失败时调用 StepAudio 在线备用；也可显式选择仅本地或仅在线。用户确认后由当前文本大模型结合完整问题、上下文和命中片段生成可口述答案。完整录音及会后转写保持本地处理。
- 实时回答提供极速本地、智能混合和高质量云端三种模式。智能混合使用可编辑 AI 术语词典保守纠正语音错词，并保留原文和纠正痕迹；发生纠正、存在未知缩写或知识库没有直接命中时路由至 Step 云端，否则使用本地 llama.cpp。用户可对本地答案发起云端重新回答。模型不得在没有可靠依据时猜测技术缩写的全称或用途。
- 录音期间始终显示红色状态与计时，关闭窗口或退出程序时立即停止录音。

## 7. 信息架构

| 板块 | 核心问题 | 内容生命周期 |
|---|---|---|
| 提醒 | 什么时候需要再次关注？ | 待触发、已通知、完成或滚动到下一周期 |
| 便签 | 近期需要记住或处理什么？ | 创建、编辑、待办/已完成筛选、删除 |
| 笔记 | 哪些知识需要长期积累？ | 持续编辑、分类、检索、待办/已完成筛选 |
| 新闻 | 有哪些公开 AI 动态？ | 只读加载、查看原文、刷新 |
| 设置 | 如何调整应用行为和本地模型？ | 本地配置、即时保存 |

五个板块使用顶部胶囊导航切换。设置内容直接位于顶部导航下方，不重复显示页面标题；日历属于提醒内部能力。

## 8. 功能优先级

| 优先级 | 定义 | 功能 |
|---|---|---|
| P0 | 核心闭环不可缺少 | 本地保存、便签、笔记、提醒调度、托盘驻留、数据恢复 |
| P1 | 显著提升体验 | 双历日历、周期提醒、分类标签、全文搜索、学习统计 |
| P2 | 后续增强 | 数据导入导出、多显示器完善、提醒历史、批量操作 |
| 暂不做 | 当前不进入开发 | 云同步、多人协作、移动端、复杂项目管理 |

## 9. 提醒板块需求

### 9.1 提醒来源

- 独立提醒。
- 便签关联提醒。

### 9.2 新建提醒

点击“新建提醒”后，应先打开双历日历选择器，再填写标题和说明。

支持：

- 阳历、农历、传统节日和二十四节气。
- 2026 年法定放假“休”和调休“班”标记。
- 日期及时分选择。
- 单次、每天、每周、每月、每年、工作日提醒。
- 指定星期、间隔周期、提前提醒、结束日期。

### 9.3 提醒操作

- 完成：单次提醒进入已完成；周期提醒计算下一次。
- 稍后：默认延后 30 分钟。
- 跳过本次：周期提醒进入下一周期。
- 暂停/恢复：暂停后不得触发通知。
- 打开来源：跳转到对应便签或笔记。
- 删除：删除提醒；删除来源内容时同步删除关联提醒。

### 9.4 通知规则

- 应用隐藏时优先发送 Windows 系统通知。
- 系统通知失败时回退到应用内弹窗。
- 同一周期不得重复通知。
- 全局暂停提醒后，不触发任何到期通知。

## 10. 便签板块需求

便签用于短期备忘，强调快速创建和快速处理。

字段：

- 标题。
- 多行正文。
- 局部粗体格式。
- 可选提醒时间和规则。
- 创建时间、更新时间。

交互：

- 点击新建后打开编辑对话框，保存后才创建。
- 标题为空时使用“无标题”。
- 取消时不得产生空便签。
- 支持 `Ctrl+B`、`Ctrl+Z`、`Ctrl+Y`。
- 双击列表项可重新打开编辑对话框。
- 主界面默认展示“待办”视图；点击“已完成”只展示当前标题下已勾选的任务块。
- 在待办视图点击任务行左侧 `☐` 后，任务标记改为 `☑`，正文及后续说明保留在原标题中。
- 编辑弹窗展示完整正文，可新增待办或切换 `☐ / ☑`。
- 正文编辑后自动保存。

## 11. 笔记板块需求

笔记用于长期知识积累。

笔记不设置提醒，专注于长期知识积累。除基础编辑字段外，还包括：

- 单一分类。
- 多个标签。
- 学习积累记录。

支持：

- 新建分类。
- 按全部分类、未分类或指定分类筛选。
- 按标题、正文、分类和标签搜索。
- 标记和撤销“今日已积累”。
- 查看连续天数、本周次数、本月次数和累计次数。
- 在日历中查看历史积累日期。
- 与便签一致支持待办/已完成筛选；分类、标签和全文检索仍以完整正文为准。

## 12. 双历日历需求

- 采用周一作为每周第一天。
- 始终显示 6 行、42 个日期格。
- 阳历日期为主，农历或节气为辅。
- 当前日期、选中日期和非本月日期应有明显层级。
- 节气和传统节日优先于普通农历日期显示。
- 法定休息日显示“休”，调休工作日显示“班”。
- 有提醒的日期显示提示圆点。
- 支持上月、下月、回到今天和鼠标滚轮切月。
- 未正式公布年度调休安排时不得自行推测“休/班”。

## 13. 桌面窗口需求

### 13.1 窗口表现

- 支持黄色、蓝色、无色、深色和“图片皮肤”。
- 图片皮肤从用户选择的本地图片提取调色板，替换背景、面板、输入框、强调色和文字色；源图片复制至 `D:\QingjianData\轻笺\skins`。
- 支持透明度调节。
- 鼠标进入时提高可读性，离开后恢复设定透明度。
- 支持窗口置顶。
- 支持自定义标题栏、拖动、最小化和关闭。

### 13.2 贴边隐藏

- 左侧贴边向左隐藏。
- 右侧贴边向右隐藏。
- 顶部贴边向上隐藏。
- 距离边缘 12px 内判定贴边，隐藏后保留约 7px 感应区域。
- 依据窗口当前所在显示器的工作区判断三边，支持主屏左侧负坐标。
- 动态识别显示器接入、移除和工作区变化；拔屏后自动回收不可见窗口。
- 录音转写次级窗口遵循相同贴边隐藏规则。
- 录音转写窗口不独占主窗口输入；录音期间仍可移动主窗口、切换条目和编辑正文。
- 界面字号提供紧凑、标准、较大三档，作为 Per-Monitor DPI 自动缩放之外的个人倍率；主窗口、录音窗口与便签/笔记编辑弹窗均按所在显示器独立缩放。
- 鼠标触碰对应边缘后恢复窗口。
- 调整窗口尺寸期间暂停自动隐藏。

### 13.3 窗口缩放

- 左、右和底部边界可拖动。
- 左下角、右下角可双向缩放。
- 最小尺寸为 420×480。
- 松开鼠标后保存窗口尺寸和位置。

## 14. 系统托盘与启动

- 点击关闭按钮默认隐藏到系统托盘，不退出进程。
- 双击托盘图标恢复窗口。
- 托盘菜单提供显示、新建三类内容、暂停提醒和退出。
- 只有选择“退出”才完全结束程序。
- 用户可在设置中启用或关闭当前用户级开机启动。
- 开机启动失败时应提示，不得显示已成功状态。

## 15. 数据模型与状态

### 15.1 主要实体

- `note`：便签或笔记，通过 `kind` 区分。
- `reminder`：统一提醒记录，可通过 `source_id` 关联便签；笔记不关联提醒。
- `category`：笔记分类。
- `learning_log`：笔记积累日期集合。
- `completed_tasks`：旧版误归档任务的恢复中间数据；当前任务状态以正文中的 `☐ / ☑` 标记为准。
- `settings`：窗口、主题、图片皮肤、提醒和启动设置。

### 15.2 提醒状态

```text
pending → notified → completed
    ↓          ↓
  paused     snooze → pending
```

周期提醒完成或跳过后，若未超过结束日期，则重新进入 `pending` 并生成下一次时间。

### 15.3 数据存储

- 数据路径：`%APPDATA%\轻笺\data.json`。
- 保存时使用临时文件和原子替换，降低写入中断风险。
- 数据结构升级时自动补充缺省字段。
- 旧版任务误归档数据首次加载时按来源回填、同源去重；来源冲突记录保留为待人工确认，不自动猜测归属。
- 无法解析的数据文件应保留为 `.broken.json`，不得直接覆盖。

## 16. 核心流程

### 16.1 新建便签

```text
进入便签 → 新建 → 输入标题/内容 → 可选提醒 → 保存 → 列表出现新便签
```

### 16.2 新建独立提醒

```text
进入提醒 → 新建 → 选择日期时间 → 输入标题 → 设置周期 → 保存 → 提醒中心展示
```

### 16.3 学习积累

```text
进入笔记 → 选择笔记 → 今日已积累 → 写入当天记录 → 更新连续和周期统计
```

### 16.4 关闭与退出

```text
点击关闭 → 保存数据 → 隐藏到托盘 → 继续调度提醒
托盘选择退出 → 保存数据 → 移除托盘图标 → 结束进程
```

## 17. 异常与边界处理

- 提醒时间格式错误时阻止保存并提示正确格式。
- 首次提醒时间早于当前时间时阻止创建。
- 没有撤销或重做历史时不得报错。
- 删除操作需要二次确认。
- 找不到提醒来源时，“打开来源”不执行跳转。
- 农历换算失败时仍应显示阳历，不影响创建提醒。
- Windows 通知失败时使用应用内弹窗。
- 托盘初始化失败时，关闭窗口应允许正常退出，避免后台失控。
- 年度节假日数据未提供时，只显示农历、传统节日和节气。

## 18. 非功能需求

| 类别 | 要求 |
|---|---|
| 离线能力 | 核心记录、检索和提醒不依赖网络 |
| 数据安全 | 保存失败不得破坏上一次有效数据 |
| 启动性能 | 常规本地数据量下，目标 3 秒内出现主窗口 |
| 提醒检查 | 正常运行时每 15 秒检查一次到期提醒 |
| 兼容性 | 优先支持 Windows 10/11、Python 3.8 环境 |
| 显示适配 | 支持 Per-Monitor DPI、多显示器负坐标和显示器热插拔 |
| 可维护性 | 产品、技术和运行文档分离；关键逻辑有自动测试 |
| 隐私 | 默认不上传内容，不接入用户行为分析服务 |

## 19. 验收标准

### AC-01 数据保存

新建便签并关闭到托盘后重新打开，标题、正文和格式保持不变；完全退出并重新启动后数据仍存在。

### AC-02 单次提醒

创建未来时间的单次提醒后，提醒中心显示该事项；到达时间时产生系统通知或应用内回退通知；完成后状态为已完成且不再触发。

### AC-03 周期提醒

创建每月提醒并完成本次后，下一次日期按月推进；遇到不存在的日期时使用当月最后一天。

### AC-04 关联来源

从便签创建提醒后，在提醒中心点击“打开来源”能够进入对应便签；删除便签后关联提醒同步删除。

### AC-05 日历

2026年7月23日应显示农历六月初十和大暑；2026年10月1日应标记国庆放假；10月10日应标记调休工作日。

### AC-06 分类检索

给笔记设置分类和标签后，可通过分类筛选和关键词搜索找到该笔记；搜索范围包括标题、正文、分类和标签。

### AC-07 学习统计

连续两天标记同一篇笔记“今日已积累”后，连续天数为2；重复点击同一天不得重复计数。

### AC-08 贴边隐藏

窗口贴近左、右或顶部且鼠标离开后向对应方向隐藏；触碰保留区域后恢复到屏幕内。

### AC-09 托盘

点击关闭后主窗口消失但进程继续；双击托盘图标恢复；选择托盘“退出”后进程与托盘图标均消失。

### AC-10 数据迁移

旧版本数据首次加载后自动补充新版字段，原有标题和正文不丢失。

## 20. 产品指标

当前不上传统计数据，仅定义本地可观察指标：

- 有效便签数量。
- 有效笔记数量。
- 提醒完成率和逾期数量。
- 每周学习积累天数。
- 启动失败和数据解析失败次数。

后续若引入数据分析，必须默认关闭并获得用户明确授权。

## 21. 版本路线图

### V0.2：当前基线

- 提醒、便签、笔记核心功能。
- 双历和高级提醒。
- 分类标签与学习统计。
- 系统通知、托盘和开机启动。

### V0.3：紧凑模式与快捷边条

- 笔记取消提醒，既有笔记提醒迁移为独立提醒。
- 最小窗口目标为 320×420，窄窗口自动折叠列表。
- 提醒、便签、笔记均可固定到伴随式快捷边条。
- 快捷边条不提供“+”按钮，仅支持把条目拖入固定；各标签独立竖排，点击打开、右键移除。
- 快捷标签统一尺寸且仅显示标题文字；鼠标悬停时以柔和的明暗呼吸动画反馈当前标签。
- 列表拖拽超过 10px 后才启动，并锁定按下时的条目；列表右键提供“固定到快捷标签”作为可靠替代入口。
- 快捷标签统一通过“板块类型 + 条目 ID”定位内容；同板块直接切换条目，跨板块仅同步必要布局，不经过默认首项。
- 主体采用响应式分栏：宽屏标题列表在左、正文在右；窄屏正文在上、标题列表在下，分隔线均可拖动。
- 各板块工具栏按可见操作数量等宽分布，窄屏自动切换短标题，避免按钮挤压或裁切。
- 笔记编辑器按当前分类汇总历史标签并显示为可换行卡片；单击复用或取消，双击／右键可在该分类范围内重命名并同步已有笔记。
- 笔记板块工具栏下仅提供“AI面试复盘”入口；可基于当前面试转写生成摘要、提取问答与STAR案例、分析不足及下一轮准备。结果必须预览确认后复制、追加或生成独立笔记，禁止自动覆盖原文。
- AI模型使用OpenAI兼容接口并在后台调用；长文本自动分段归纳，过滤模型思考标记。接口地址与模型名保存在普通设置中，Token仅保存于Windows凭据管理器。
- 分类筛选项以现有笔记实际使用的分类为唯一来源；启动及增删改后自动补齐缺失分类并清理无笔记使用的历史残留分类。
- 窄屏标题列表按内容自适应固定高度，窗口向下扩展时新增空间优先交给正文；宽屏列表宽度参考最长标题并限制合理范围。
- 设置作为顶部一级板块；进入设置时隐藏条目列表和正文，但保留顶部板块导航；取消底部可视保存状态栏，保留后台自动保存。
- 增加“新闻”板块，通过 AI HOT 匿名只读 `/api/public/daily` 接口加载日报；以北京时间 08:00 为新闻日期切换点，08:00 前继续复用昨日缓存，08:00 后首次进入时获取并覆盖为当日缓存。手动刷新也遵守此日期边界，新闻缓存不写入便签业务数据文件。
- 新闻保留 AI HOT 署名、站内链接和第三方原文入口，不允许编辑、删除、提醒或固定到快捷标签。
- 当前板块标题列表支持 `↑` / `↓` 即时切换正文；顶部板块使用 `←` / `→` 循环切换，可见操作按钮支持四方向空间导航。
- 输入框、正文编辑器、下拉框和滑块保留原生方向键行为，不被全局导航劫持。
- 快捷边条从属于主窗口；主窗口最小化、关闭到托盘或贴边隐藏时必须同步隐藏。
- 应用只允许启动一个实例；重复启动时唤起已经运行的主窗口，不复制界面。

### 当前实现补充（2026-08-23）

- 顶部统一为提醒、便签、笔记、新闻、设置五大板块；设置页与顶部导航直接对齐，不再显示重复的“设置”标题。
- 便签和笔记的正文任务以 `☐ / ☑` 标记为唯一状态来源：默认“待办”隐藏已完成项，“已完成”隐藏未完成项，并保留完成任务后的说明文字。
- 在待办正文中直接点击 `☐` 即可完成该项；编辑弹窗始终展示完整正文，避免筛选视图影响编辑。
- 兼容旧版错误归档的任务数据：启动时按原始来源回填并同源去重；来源不明确时保留待人工确认，禁止自动猜测归属。
- 设置页支持 PNG、JPG、JPEG、BMP、WEBP 图片皮肤；原图片副本存放在 `D:\QingjianData\轻笺\skins`，由图片主色生成全界面配色，并可恢复纯色皮肤。
- 主窗口及便签、笔记编辑弹窗使用按显示器 DPI 的缩放策略，支持跨外接屏移动后的重新适配。

### V0.3.1：稳定性

- 持续补充特殊显卡驱动、混合缩放比例和远程桌面场景的显示兼容测试。
- 数据备份、恢复、导入和导出。
- 提醒历史与故障恢复。
- 托盘和 Windows 通知专项兼容测试。

### V0.4：当前体验基线

- 顶部五板块导航与设置页面内嵌布局。
- 便签、笔记任务标记、待办/已完成筛选与历史误归档恢复。
- 本地图片取色皮肤。
- 编辑弹窗跨屏 DPI 同步。
- 批量操作和更完整的搜索。
- 标签颜色管理。
- 日历列表联动优化。
- 键盘无障碍和快捷操作完善。

### V1.0：可分发版本

- 打包为独立安装程序。
- 自动升级或手动版本检查。
- 数据迁移保障和卸载保留策略。
- 完整用户帮助、隐私说明和发布验收。

## 22. 风险与待确认事项

1. Windows 通知在不同系统版本和通知设置下的行为需要真机专项验证。
2. 特殊显卡驱动、远程桌面和混合缩放比例仍需要真机专项验证。
3. 年度法定节假日需要根据国务院办公厅通知持续更新。
4. JSON 单文件在数据量持续增大后可能需要迁移到 SQLite。
5. Tkinter 对亚克力、圆角和复杂动画的支持有限，是否迁移 PySide6 需在 V0.4 前评估。
6. 待办/已完成视图与旧版误归档恢复已实现；来源冲突的历史任务仍需人工确认归属。
7. 是否增加数据加密、密码保护和自动备份，需要进一步确认。

## 23. 产品决策记录

| 日期 | 决策 | 原因 |
|---|---|---|
| 2026-07 | 将提醒、便签、笔记拆分 | 区分时间管理、短期备忘和长期积累 |
| 2026-07 | 提醒采用统一数据模型 | 避免三个板块维护三套提醒逻辑 |
| 2026-07 | 日历只属于提醒内部能力 | 避免一级导航过多和工具栏拥挤 |
| 2026-07 | 关闭默认进入托盘 | 保证提醒调度持续运行 |
| 2026-07 | 核心功能保持本地优先 | 降低依赖并保护个人数据 |
