# 轻笺使用说明

轻笺是一款本地优先的 Windows 桌面便签工具，提供提醒、便签、笔记、AI 新闻、录音转写和 AI 总结等功能。

> 本文面向普通使用者。开发环境、源码结构和测试方式请参阅 [README.md](README.md)。

## 一、安装准备

### 基础功能

1. 使用 Windows 10 或 Windows 11。
2. 安装 Python 3.8 或更高版本，并在安装时勾选“Add Python to PATH”。
3. 将轻笺放在 D、E 等非系统盘目录中。
4. 双击 `run.bat` 启动。

基础的提醒、便签、笔记和新闻功能不需要安装第三方 Python 包。

### 录音功能

需要录制麦克风或电脑会议声音时，先双击 `install_voice.bat`。脚本会在项目目录的 `runtime\app_env` 中创建独立环境并安装录音组件，不修改源码，也不把下载缓存放到 C 盘。

### 本地语音转写

本地 SenseVoice 转写需要额外安装运行环境和模型：

1. 双击 `install_sensevoice.bat`。
2. 准备以下官方模型，并保持目录名称不变：
   - `models\SenseVoiceSmall`
   - `models\fsmn-vad`
   - `models\campplus-speaker`
3. 再次启动轻笺，在“笔记 → 录音”中检查模型状态。

模型体积较大，不包含在 Git 源码仓库中。请从模型官方页面取得并遵守各模型许可证。

## 二、首次启动

双击 `run.bat` 后，轻笺会显示主窗口，并在 Windows 托盘区创建图标。

- 关闭主窗口：隐藏到托盘，提醒仍继续运行。
- 完全退出：右击托盘图标，选择“退出”。
- 修改程序文件后重新加载：右击托盘图标，选择“刷新轻笺”。
- 重复双击 `run.bat`：只会唤起已经运行的窗口。

## 三、四个工作区

### 提醒

- 点击“新建”创建独立提醒。
- 选择日期和时间，可设置单次、每天、每周、每月、每年、工作日或间隔提醒。
- 提醒触发后可以完成、稍后 30 分钟或打开来源。
- 轻笺必须保持运行；隐藏到托盘不影响提醒。

### 便签

- 用于短期备忘。
- 双击条目可以编辑标题和正文。
- 正文支持换行、加粗、撤销 `Ctrl+Z` 和重做 `Ctrl+Y`。

### 笔记

- 用于长期知识积累。
- 支持分类、标签、筛选和全文搜索。
- 同一分类中可以复用已有标签，并可修改标签名称。
- 笔记可进行“AI 面试复盘”或“AI 会议总结”。

### 新闻

- 顶部可切换“AI 日报”和“免费模型快讯”。
- AI 日报展示 AI HOT 每日资讯，北京时间 08:00 后首次打开时联网更新。
- 免费模型快讯只收录可直接调用的在线免费 API，不收录需要下载或自行部署的模型；北京时间 09:00 后首次打开时读取当天快讯。
- 两类新闻均只保留一个本地缓存文件，每日覆盖前一天内容，不形成历史记录。
- 每条快讯标注免费期限、模型特点和适用场景；若平台没有公布截止日期，界面会明确提示“可能随时调整”。
- 北京时间每天 08:00 后第一次进入时联网更新，此后使用当天缓存。
- 08:00 前继续展示上一日缓存。
- 使用 `↑`、`↓` 可以切换新闻，支持打开 AI HOT 页面或新闻原文。

## 四、快捷操作

- `←`、`→`：切换顶部工作区。
- `↑`、`↓`：切换当前标题列表条目。
- `Enter` 或空格：执行当前聚焦按钮。
- 将提醒、便签或笔记拖到窗口侧边：创建快捷标签。
- 右击快捷标签：移除快捷标签，不删除原内容。

## 五、窗口设置

在“设置”中可以调整：

- 窗口置顶
- 贴边隐藏
- 开机启动
- 窗口透明度和色彩主题
- 界面字号

将主窗口或录音窗口拖到当前显示器左侧、右侧或顶部，鼠标离开后会自动隐藏；触碰对应屏幕边缘即可弹出。多显示器之间可以直接拖动，轻笺会根据各显示器 DPI 自动缩放。

## 六、AI 配置

AI 总结和问答需要用户自己的兼容接口凭据：

1. 打开“设置”。
2. 填写接口地址、模型名称和 API Key。
3. 点击“测试 LLM”。
4. 测试成功后点击“保存配置”。

API Key 保存到当前 Windows 用户的凭据管理器，不写入轻笺数据文件，也不会随 Git 仓库共享。

## 七、录音与转写

1. 进入“笔记”，点击“录音”。
2. 选择“我的麦克风”和实际播放会议声音的耳机或扬声器回环设备。
3. 点击“开始录音”。
4. 完成后点击“停止并转写”。
5. 转写结果可以复制、追加到当前笔记或生成独立笔记。

录音前应确认参与者已经知情并同意。不要录制涉密会议，也不要把包含个人信息或商业秘密的内容上传到未经授权的云端接口。

## 八、数据位置与备份

个人数据默认保存在：

```text
%APPDATA%\轻笺
```

其中可能包括便签数据、新闻缓存、知识库索引和录音文件。更换电脑前可以退出轻笺后备份该目录。API Key 位于 Windows 凭据管理器，需要在新电脑重新配置。

## 九、常见问题

### 双击 run.bat 后没有反应

在项目目录打开 PowerShell，执行：

```powershell
python main.py
```

根据控制台错误检查 Python 是否已加入 PATH。也可以确认任务栏托盘中是否已经运行轻笺。

### 录音设备列表为空

先运行 `install_voice.bat`，然后刷新设备；同时检查 Windows 麦克风权限和声卡驱动。

### 本地转写提示模型缺失

确认三个模型目录和文件完整，并重新运行 `install_sensevoice.bat`。模型不能只放压缩包，必须解压到 `models` 下的对应目录。

### AI 连接失败

确认接口地址、模型名称、API Key、网络和账户额度。测试失败后可以修改配置并重新点击测试，不需要退出软件。

### 两个显示器上的大小不同

轻笺会按照显示器 DPI 自动缩放。可在设置中调整“界面字号”，然后通过托盘“刷新轻笺”重新加载。
